# Results

- `published/` contains immutable historical score and accuracy JSON for Tables 1--3.
- `reproduced/` contains regenerated reports and CSV tables.
- `research/` is for explicitly named corrected runs and future methods.

Historical and corrected results must never share a filename or overwrite each other.
